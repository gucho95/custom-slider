export type Position = number;
export type Positions = Position[];

export type Breakpoint = {
  label: number;
  labelPositionInPercent: number;
  matchRange: number[];
};

export type Breakpoints = Breakpoint[];
